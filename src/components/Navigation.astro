---
import { CONTACT_EMAIL, CONTACT_HOURS, CONTACT_PHONE_DISPLAY, CONTACT_PHONE_HREF, DASHBOARD_URL, SOLUTION_LINKS } from '../config';
---

<header class="sticky top-0 z-50 bg-white/95 backdrop-blur-sm border-b border-secondary-100">
  <div class="bg-secondary-950 text-secondary-100">
    <div class="section-container flex h-9 items-center justify-end gap-2 text-xs">
      <a href={`tel:${CONTACT_PHONE_HREF}`} class="font-semibold hover:text-white">{CONTACT_PHONE_DISPLAY}</a>
      <span aria-hidden="true">/</span>
      <span class="sm:hidden">Mo bis Fr, 8 bis 17 Uhr</span>
      <span class="hidden sm:inline">{CONTACT_HOURS}</span>
    </div>
  </div>
  <nav class="section-container flex items-center justify-between h-16 md:h-18">
    <a href="/" class="flex items-center gap-2 shrink-0">
      <img src="/logo.svg" alt="Zimmero Logo" width="32" height="32" class="w-8 h-8" />
      <span class="text-xl font-semibold text-secondary-900">Zimmero</span>
    </a>

    <div class="hidden xl:flex items-center gap-8">
      <a href="/#funktionen" class="text-sm font-medium text-secondary-600 hover:text-primary-600 transition-colors">Funktionen</a>
      <details class="group relative">
        <summary class="inline-flex cursor-pointer list-none items-center gap-1 text-sm font-medium text-secondary-600 transition-colors hover:text-primary-600 [&::-webkit-details-marker]:hidden">
          Lösungen
          <svg class="h-4 w-4 transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m6 9 6 6 6-6" />
          </svg>
        </summary>
        <div class="absolute left-1/2 top-full mt-3 w-64 -translate-x-1/2 rounded-xl border border-secondary-200 bg-white p-2 shadow-xl">
          {SOLUTION_LINKS.map(link => (
            <a href={link.href} class="block rounded-lg px-4 py-3 text-sm font-medium text-secondary-700 transition-colors hover:bg-primary-50 hover:text-primary-700">{link.label}</a>
          ))}
        </div>
      </details>
      <a href="/#zielgruppe" class="text-sm font-medium text-secondary-600 hover:text-primary-600 transition-colors">Für wen</a>
      <a href="/preise/" class="text-sm font-medium text-secondary-600 hover:text-primary-600 transition-colors">Preise</a>
      <a href="/#faq" class="text-sm font-medium text-secondary-600 hover:text-primary-600 transition-colors">FAQ</a>
    </div>

    <div class="hidden xl:flex items-center gap-3">
      <a href="/kontakt/" class="btn-secondary text-sm py-2 px-4">Zimmero anfragen</a>
      <a href={DASHBOARD_URL} target="_blank" rel="noopener" class="btn-primary text-sm py-2 px-4">Dashboard öffnen</a>
    </div>

    <button
      id="mobile-menu-btn"
      class="xl:hidden p-2 -mr-2 text-secondary-600 hover:text-secondary-900"
      aria-label="Menü öffnen"
      aria-expanded="false"
    >
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
      </svg>
    </button>
  </nav>

  <div id="mobile-menu" class="hidden xl:hidden border-t border-secondary-100 bg-white">
    <div class="section-container py-4 space-y-3">
      <a href="/#funktionen" class="block text-sm font-medium text-secondary-600 hover:text-primary-600 py-2">Funktionen</a>
      <details class="group">
        <summary class="flex cursor-pointer list-none items-center justify-between py-2 text-sm font-medium text-secondary-600 hover:text-primary-600 [&::-webkit-details-marker]:hidden">
          Lösungen
          <svg class="h-4 w-4 transition-transform group-open:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m6 9 6 6 6-6" />
          </svg>
        </summary>
        <div class="mt-1 space-y-1 border-l border-secondary-200 pl-4">
          {SOLUTION_LINKS.map(link => (
            <a href={link.href} class="block py-2 text-sm text-secondary-600 hover:text-primary-700">{link.label}</a>
          ))}
        </div>
      </details>
      <a href="/#zielgruppe" class="block text-sm font-medium text-secondary-600 hover:text-primary-600 py-2">Für wen</a>
      <a href="/preise/" class="block text-sm font-medium text-secondary-600 hover:text-primary-600 py-2">Preise</a>
      <a href="/#faq" class="block text-sm font-medium text-secondary-600 hover:text-primary-600 py-2">FAQ</a>
      <div class="pt-3 border-t border-secondary-100 flex flex-col gap-2">
        <a href="/kontakt/" class="btn-secondary text-sm text-center">Zimmero anfragen</a>
        <a href={`mailto:${CONTACT_EMAIL}?subject=Anfrage%20zu%20Zimmero`} class="text-center text-sm font-medium text-primary-700 py-2">Direkt per E-Mail schreiben</a>
        <a href={DASHBOARD_URL} target="_blank" rel="noopener" class="btn-primary text-sm text-center">Dashboard öffnen</a>
      </div>
    </div>
  </div>
</header>

<script>
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  btn?.addEventListener('click', () => {
    const expanded = btn.getAttribute('aria-expanded') === 'true';
    btn.setAttribute('aria-expanded', String(!expanded));
    menu?.classList.toggle('hidden');
  });

  document.querySelectorAll('#mobile-menu a').forEach(link => {
    link.addEventListener('click', () => {
      menu?.classList.add('hidden');
      btn?.setAttribute('aria-expanded', 'false');
    });
  });
</script>
